# 开发规范与协作指南

> 本文档定义项目的开发流程、Git 工作流和代码提交规范。所有贡献者必须遵守。

---

## 📋 Git 工作流

### 1. 分支策略

| 分支类型 | 命名规范 | 说明 |
|:-------:|---------|------|
| **主分支** | `main` | **只读**，禁止直接提交 |
| **任务分支** | `block{N}-{描述}` | 如 `block0-method-alignment` |
| **文档分支** | `docs-{描述}` | 如 `docs-cleanup` |
| **修复分支** | `fix-{描述}` | 如 `fix-shap-color` |

### 2. 核心规则

#### 🔒 主分支只读
```bash
# ❌ 禁止
git checkout main
git commit -m "xxx"

# ✅ 正确做法
git checkout -b block1-inversion-engine
# 在任务分支上工作
```

#### 🎯 任务分支纯粹
- **一个分支只做该 Block 的事情**，不跨 Block 修改
- 如需处理旁路任务（docs/cleanup 等），**新建语义清晰的分支**

```bash
# 当前在 block1 分支，需要临时处理文档
git stash push -u -m "block1: 暂存反演引擎修改"
git checkout -b docs-readme-update
# 完成文档任务后
git checkout block1-inversion-engine
git stash pop
```

#### 📝 提交规范
- **Commit message 必须使用中文**
- **具体描述"做了什么"**，禁止模糊描述
- **每次 commit 立即 push 到远程**

```bash
# ❌ 错误示例
git commit -m "fix"
git commit -m "update"
git commit -m "修改了一些东西"

# ✅ 正确示例
git commit -m "修复 SHAP 图配色问题，统一使用灰阶"
git commit -m "添加 LP 反演引擎的边界检查逻辑"
git commit -m "更新 README 的快速开始章节"
git push origin block1-inversion-engine
```

### 3. Stash + 分支切换流程

当任务未完成但需要切换到其他工作时：

```bash
# Step 1: 暂存当前工作（包括未追踪文件）
git stash push -u -m "block1: 描述当前进度"

# Step 2: 切换到新分支处理其他任务
git checkout -b docs-cleanup
# ... 完成任务 ...
git add .
git commit -m "清理冗余文档，整理目录结构"
git push origin docs-cleanup

# Step 3: 回到原分支继续工作
git checkout block1-inversion-engine
git stash pop
```

### 4. 合并流程

#### 验收前检查清单
- [ ] 本 Block 的所有功能已完成并测试
- [ ] 代码仅涉及本 Block，无跨 Block 修改
- [ ] 对已完成 Block 无影响
- [ ] 相关文档已更新

#### 合并步骤
```bash
# 1. 确保分支最新
git checkout main
git pull origin main
git checkout block1-inversion-engine
git rebase main

# 2. 请求合并（通过 PR 或告知负责人）
# 3. 合并后打 tag（由负责人操作）
git tag -a v0.1.0-block1 -m "完成 Block1 反演引擎"
```

### 5. 异常处理

**如发现以下情况，必须停下来确认：**
- 任务描述不清晰
- 需要跨 Block 修改
- 可能影响已完成的 Block
- 发现之前 Block 的 Bug

**处理方式：**
1. 停止当前修改
2. 记录问题到 issue 或文档
3. 与团队确认后再继续

---

## 📁 目录规范

### 核心目录结构

```
ProblemC-1/
├── src/                 # 核心代码（模型、引擎、分析）
├── scripts/             # 执行脚本（pipeline、工具）
├── data/                # 数据文件（只读）
├── docs/                # 文档（开发文档、算法说明）
├── paper/               # 论文源文件
├── figures/             # 生成的图表
├── outputs/             # 分析结果输出
└── archive/             # 归档文件（历史版本、废弃代码）
```

### 文件命名规范

| 类型 | 规范 | 示例 |
|-----|------|-----|
| Python 模块 | 小写 + 下划线 | `lp_percent.py` |
| 图表文件 | `fig_{question}_{name}.pdf` | `fig_q1_hdi_band.pdf` |
| 数据文件 | 描述性名称 | `fan_vote_intervals.csv` |
| 文档文件 | 中文/英文均可 | `模型一_算法设计.md` |

---

## 🔧 代码规范

### Python 代码风格
- 遵循 PEP 8
- 使用 type hints
- 函数必须有 docstring
- 复杂逻辑添加注释

### 图表生成规范
- 统一使用 `paper_palette.py` 配色
- 输出格式：PDF（矢量）+ PNG（2x 分辨率）
- 字体：优先 Arial/Helvetica

---

## 📌 Quick Reference

```bash
# 常用命令速查
git stash push -u -m "描述"    # 暂存（含未追踪）
git stash pop                   # 恢复暂存
git stash list                  # 查看暂存列表
git branch -a                   # 查看所有分支
git log --oneline -10           # 查看最近10条提交
```

---

*最后更新：2026-02-01*
